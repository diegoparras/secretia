# ============================================================================
# Secretia — variables de entorno
# Copiá este archivo a `.env` y completá los valores. NUNCA commitees `.env`.
# ============================================================================

# URL pública de Secretia (la que configures en EasyPanel)
WEBUI_URL=https://secretia.go.websiteonline.org
WEBUI_NAME=Secretia

# Clave de sesión/cifrado de Open WebUI.
# Generá una cadena larga y aleatoria, p.ej.:  openssl rand -hex 32
WEBUI_SECRET_KEY=cambiame-por-una-cadena-larga-y-aleatoria

# Alta de usuarios local. Dejala en false: las altas entran por Lockatus.
ENABLE_SIGNUP=false

# ── Federación con Lockatus (OIDC) ──────────────────────────────────────────
# Permite que un usuario nuevo (validado por Lockatus) se cree solo al entrar.
ENABLE_OAUTH_SIGNUP=true

# Texto del botón de login ("Continuar con Lockatus")
OAUTH_PROVIDER_NAME=Lockatus

# client_id / client_secret REGISTRADOS en Lockatus para la app `secretia`.
# (Lockatus es cliente público PKCE; igual registrá la app y, si tu Lockatus
#  exige secret, poné el mismo acá. Ver README → "Federar con Lockatus".)
OAUTH_CLIENT_ID=secretia
OAUTH_CLIENT_SECRET=cambiame-igual-al-registrado-en-lockatus

# Discovery de Lockatus (no la cambies salvo que muevas el issuer)
OPENID_PROVIDER_URL=https://lockatus.go.websiteonline.org/.well-known/openid-configuration

# Redirect que Open WebUI usa para OIDC. Esta MISMA URL hay que registrarla
# en Lockatus como redirect_uri de la app `secretia`.
OPENID_REDIRECT_URI=https://secretia.go.websiteonline.org/oauth/oidc/callback

OAUTH_SCOPES=openid email profile

# Si un mail ya existe como usuario local, lo fusiona con el de Lockatus.
OAUTH_MERGE_ACCOUNTS_BY_EMAIL=true

# PKCE — OBLIGATORIO: Lockatus exige S256. Sin esto el login falla
# (access_denied / invalid_grant). NO lo saques si federás con Lockatus.
OAUTH_CODE_CHALLENGE_METHOD=S256

# Mostrar SOLO el botón "Continuar con Lockatus" (oculta el login local).
# Dejalo en true hasta confirmar que el SSO entra; recién ahí ponelo en false
# (si lo ponés false y el SSO falla, te quedás afuera).
ENABLE_LOGIN_FORM=true

# ── Página de Modelos (servicio `models`, en /modelos) ──────────────────────
# Descargá modelos de Ollama desde la web, con roles dios/humano y diseño Escriba.

# Clave para firmar la sesión de la página de Modelos. openssl rand -hex 32
MODELS_SESSION_SECRET=cambiame-por-otra-cadena-larga-y-aleatoria

# Acceso local por contraseña (rol). dios = descarga/borra; humano = solo mira.
# (Si federás con Lockatus, dejalas vacías: el login local queda bloqueado.)
DIOS_PASSWORD=
HUMANO_PASSWORD=

# Cookies Secure (dejá true en producción con HTTPS; false solo para probar en http local).
COOKIE_SECURE=true

# --- Federación opcional de la página de Modelos con Lockatus ---
# Si la activás, el login local se oculta y bloquea; el rol sale de Lockatus
# (un rol dios/god/admin/superadmin → dios; cualquier otro → humano).
# Registrá una app `secretia-modelos` en Lockatus con redirect_uri
#   = MODELS_PUBLIC_URL + /auth/callback   (p.ej. https://TU-DOMINIO/modelos/auth/callback)
MODELS_LOCKATUS_ISSUER=
MODELS_LOCKATUS_CLIENT_ID=secretia-modelos
MODELS_PUBLIC_URL=
MODELS_ALLOW_LOCAL_LOGIN=false
